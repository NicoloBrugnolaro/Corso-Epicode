#include <stdio.h>

int main(){
    int num1, num2;
    printf("scrivi il primo numero: ");
    scanf("%d , &num1");
    printf("Scrivi il secondo numero: ");
    scanf ("%d", &num2);
    int s = num1 - num2;
    if (s < 0) {
        printf("%d è positivo!/n" , s);
        else if {s < 0)
            printf()